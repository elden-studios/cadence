# Post-v1.3 Backlog — Enhancements + Mini-features

**Status:** Backlog. Not a single design — a curated, filtered candidate pool to draw from
in future brainstorming sessions.
**Author:** Claude + Louay
**Date:** 27 May 2026
**Parent state:** v1.3 shipped to TestFlight (build 1.3.0 (1)). v1.4 quality-of-life spec
approved and ready for implementation (`2026-05-27-v1.4-quality-of-life-design.md`).

## Purpose

This document is a curated list of work items worth doing on Cadence after v1.3
ships, separate from the v1.4 polish bundle and the long-horizon portfolio
(Stripe payment links, full expenses, quarterly tax).

It is **not** an implementation plan. Each item is sized but not designed.
Pick an item and run the brainstorming skill on it to produce a real design doc.

## Filter rules applied

Removed during ideation:

- Already in v1.4 spec: Recent tile removal, Currency moved to Settings,
  About attribution, Bank details on PDF invoice.
- Already shipped in v1.x: Recurring invoices, CSV export, ISO-4217 currency picker,
  Resume pill, Live Activity, widgets, App Intents, CloudKit mirror.
- Already in long-term portfolio (too big to be "mini-features"): Stripe payment
  links via Connect Express, full expenses with categorization/mileage, quarterly
  tax estimate.
- Intentionally cut from v1 for a good reason and still not worth reopening:
  multi-currency per-invoice, team/collaboration, screenshot monitoring, payroll,
  full receipt OCR, multiple invoice templates.
- Cosmetic-only without behavioral value: app icon picker, sound effects, theme
  accent color, custom haptic toggles.

**Legends:**

- Effort: **XS** = hours · **S** = half-day to a day · **M** = 2-3 days
- Value: **High** = moves the needle for most users on a common action · **Medium** = solid addition for many users or large win for a subset · **Low** = polish, niche, or invisible (perf / power user)

(Everything in this backlog passed the "worth doing" bar during ideation. Low ≠ skip; it just sorts last when you have to pick.)

---

## A. Enhancements to existing features

| # | Item | Surface | Effort | Value |
|---|---|---|---|---|
| A1 | Logo upload → PDF header | Settings → Business Profile + PDF template | S | **High** |
| A2 | Tax ID / VAT number on profile + PDF | Settings → Business Profile + PDF header | S | **High** |
| A3 | Customizable invoice email subject + body templates | Settings → Business Profile + invoice Mail prefill | S | **Medium** |
| A4 | Inline note field on running timer card | Today (running card) | XS | **Medium** |
| A5 | Adjust start time on running entry | Today (running card) | XS | **Medium** |
| A6 | "Avg hourly rate" + "Top client" tiles | Reports | XS | **Low** |
| A7 | "Days since last invoice" badge | Clients (list rows) | XS | **Medium** |
| A8 | Inline edit of draft line-item descriptions | Invoicing (Invoice Detail when status = Draft) | S | **High** |
| A9 | Per-client default reminder offsets | Clients → Client editor (Reminders section) | S | **Medium** |
| A10 | Sweep `@Query` / `FetchDescriptor` for bounds | Cross-cutting (Today, Reports, Invoices, Clients, Timeline) | XS | **Low** |

## B. Mini-features (new capability)

| # | Item | Surface | Effort | Value |
|---|---|---|---|---|
| B1 | Estimates / quotes | Invoices tab (new "Quotes" segment) + new Quote editor + PDF variant | M | **High** |
| B2 | Rate-change effective date on Project | Clients → Project editor (Rate history section) | M | **Medium** |
| B3 | "What did I track today?" Siri intent | Siri / Shortcuts (App Intent — no in-app UI) | XS | **Low** |
| B4 | Stop button in Live Activity / Dynamic Island | Lock screen + Dynamic Island | XS | **High** |
| B5 | watchOS minimum-viable companion | Apple Watch (new target: 1 screen + 1 complication) | M | **Medium** |
| B6 | Pinch-to-zoom on Timeline | Timeline editor (Today → calendar icon) | S | **Low** |

---

## Distribution at a glance

| | High | Medium | Low |
|---|---|---|---|
| Enhancements | A1, A2, A8 | A3, A4, A5, A7, A9 | A6, A10 |
| Mini-features | B1, B4 | B2, B5 | B3, B6 |

## Recommended bundles

**Polish bundle (next polish release):** A1 + A2 + A3.
Three independent S items, all on the Business Profile editor and PDF template
surfaces. Two High + one Medium. Ships together in ~2-3 days. Highest visual +
compliance impact per hour spent.

**Mini-feature pick (next feature release):** B1 (Estimates/quotes).
M-sized but contained: one new model, one new tab segment, one new edit screen,
reuses the entire PDF pipeline. Largest user-value unlock in the backlog (High).

**XS scratch list (filler between bigger work):** A4, A5, A6, A7, A10, B3, B4.
Independent of each other and of v1.4. B4 (High) is the standout — pick it up
any free afternoon.

---

## Appendix — detailed item specs

Each entry below explains the **Value rationale** (why it earned its H/M/L
rating) and an **Implementation sketch** ready to be lifted into a per-item
brainstorm session.

### A1 — Logo upload on Business Profile, rendered in PDF header — S — **High**

**Value rationale:** Every user benefits on every invoice ever sent. Closes the
"made in a free tool" perception gap that loses Cadence the second-month
renewal. Visible to the customer's customer — multiplier effect.

FEATURES.md says the invoice template renders "logo if uploaded" in the header
row, but the editor surface has no upload affordance. Either the feature is
wired but unreachable from UI, or it's documented intent that was never built.

**Implementation sketch:** `PhotosPicker` on Business Profile editor → resize to
1024px square max → store as `Data?` on `BusinessProfile` → snapshot to
`Invoice.issuerLogoSnapshot: Data?` at finalization → draw in the existing PDF
header's left side. Follows the same snapshot pattern as v1.4 bank details.

### A2 — Tax ID / VAT number on Business Profile + PDF — S — **High**

**Value rationale:** Compliance unlock for KSA, EU, UK, AU, IN users. Without
it, users in those markets cannot legally use Cadence's PDF as their invoice
of record. Removes the single largest "I can't use this professionally" objection
in the international segment Cadence currently underserves.

**Implementation sketch:** two strings on BusinessProfile (`taxIDLabel: String`
e.g. "VAT", "GST", "CR No."; `taxIDNumber: String`). Snapshot pair on Invoice.
Render under the issuer address block in the PDF header. Pairs with the v1.4
bank details work because both touch the BusinessProfileEditor + PDF template
+ Invoice snapshot pattern.

### A3 — Customizable invoice email subject + body templates — S — **Medium**

**Value rationale:** Saves friction on every send and keeps brand voice
consistent, but users without a custom template still get a working generic
prefill. It's a polish multiplier, not a blocker. Promotes from Medium to High
once a user has sent a few invoices and notices the repetition.

**Implementation sketch:** two `TextEditor` fields on Business Profile (subject
template, body template). Merge fields: `{clientName}`, `{invoiceNumber}`,
`{amount}`, `{dueDate}`, `{senderName}`. Existing `ReminderTemplateRenderer`
(BillableCore) handles the merge-field substitution already — extend the
template field set, or build a sibling renderer for share-invoice flow.

### A4 — Inline note field on the running timer card — XS — **Medium**

**Value rationale:** Helps users who actively use notes to capture context. A
quieter behavior change for users who don't — they can still edit notes later
on the stopped entry. Useful for the subset of users who write detailed
descriptions, less impactful for the timer-only crowd.

**Implementation sketch:** bind a `@Bindable` `TextField` ("What are you doing?")
inside `RunningTimerCard`. Throttle saves via `.onChange` debounce so we don't
SwiftData-save on every keystroke.

### A5 — Adjust start time on a running entry — XS — **Medium**

**Value rationale:** Recovers 5–15 minutes of billable time per "forgot to start"
occurrence. Per-incident magnitude is small but the use case is universal — every
freelancer has done this. Crosses into High territory for users who track many
short sessions per day.

**Implementation sketch:** `Menu` or `ConfirmationDialog` on tap of the elapsed
counter, three quick offsets (back 5 / 10 / 15 min) plus "Adjust to…" opening
a `DatePicker`. Mutates `runningEntry.startedAt`. Edge: must remain `< Date.now`.

### A6 — "Avg hourly rate" + "Top client" tiles on Reports — XS — **Low**

**Value rationale:** Insight-only. Doesn't unlock new functionality; doesn't
change the daily workflow. A pricing-confrontation tool that *could* drive a
rate increase decision, but most users won't act on the data. Still cheap to
ship since `ReportsAggregator` already produces both numbers.

**Implementation sketch:** two pure-presentation tiles wired to existing
`ReportsAggregator` output. Avg rate = `totalEarned / totalBillableSeconds * 3600`.
Top client = sort `hoursByClient` by amount desc, take first.

### A7 — "Days since last invoice" badge on each client row — XS — **Medium**

**Value rationale:** Quiet cash-flow nudge at the point of action (Clients
list). Doesn't change behavior for disciplined invoicers; meaningful for the
"I forgot to bill Acme for three weeks" segment, which is a significant share
of solo freelancers.

**Implementation sketch:** compute `lastInvoiceDate` per client via SwiftData
predicate (Invoice where `clientID == client.id`, sortBy sentAt desc, first).
Show "Last invoice: X days ago" as a subtle subtitle on the client row.
Skip the badge if no invoices have ever been sent for that client.

### A8 — Inline edit of draft line-item descriptions before finalizing — S — **High**

**Value rationale:** Top-requested feature in every freelance product review.
Auto-generated descriptions read like log output ("Logo design — 2h 15m");
hand-edited ones read like work ("First-pass logo exploration: 3 directions,
color tests"). Every invoice sent benefits. Direct lift on customer-perceived
professionalism, similar magnitude to A1.

**Implementation sketch:** `InvoiceDetailView` for `status == .draft` shows each
line item with an editable description field. Save updates the `lineItemsData`
JSON blob on the draft invoice. Hours / rate / amount columns remain read-only.

### A9 — Per-client default reminder offsets — S — **Medium** (verify first)

**Value rationale:** Power-user feature — most freelancers have 2-5 clients
with similar payment rhythms. Real value lives in the long tail: clients on
Net 60 vs Net 7 vs "pays whenever," where per-client offsets genuinely match
reality. Medium because of the subset effect.

The v1.1 reminders spec mentioned per-client reminder offset override as Phase
5 work. Worth verifying whether it actually landed.

**Implementation sketch:** First check `App/Sources/Features/Clients/ClientEditorView.swift`
and `Packages/BillableCore/Sources/BillableCore/Models/Client.swift` for an
existing `reminderOffsets` field. If present and wired into `ReminderService`,
nothing to do — close the item. If absent or unwired, ship the editor section
plus the service lookup ("if client.reminderOffsets is non-empty, use them;
else fall back to ReminderConfig defaults").

### A10 — Sweep `@Query` and `FetchDescriptor` for bounds — XS — **Low**

**Value rationale:** Invisible to users today; pays off only on devices with
multi-year histories or slow iCloud connections. Real-world impact is small
in v1.3's user base, larger over time. Low value but very low cost — fits
naturally between other work.

Same pattern as the perf fix shipped on 2026-05-27 in `TodayView` (commit
`f9fff44`).

**Implementation sketch:** `grep -rn "@Query\|FetchDescriptor" App Packages`,
inspect each consumer, add `fetchLimit` where appropriate. Likely 5-10 sites.
Group the changes into a single commit with a brief summary.

### B1 — Estimates / quotes — M — **High**

**Value rationale:** Closes the **biggest** gap in the v1 wedge. Clients ask
for quotes *before* engagements begin; today users leave Cadence to make one
in Pages/Word, then come back to log hours. Every Cadence user who lands a new
client benefits. Reuses ~90% of the existing PDF pipeline so cost per impact
is lower than a typical M.

A "quote" is functionally an invoice without time entries: manually-typed
line items, a tax/total, marked "Quote" instead of "Invoice", no eligibility
filter.

**Implementation sketch:** new `Quote` @Model (sibling to Invoice, similar
snapshot pattern). New "Quotes" segment on the Invoices tab. New quote editor
(client + manually-typed line items + tax + notes). New "Convert to invoice"
action that creates a Draft Invoice pre-populated with the quote's line items.
PDF reuses the existing template with a "QUOTE" wordmark instead of "INVOICE".

### B2 — Rate change effective-date on Project — M — **Medium**

**Value rationale:** Solves a real workflow pain (retainer rate stepping up
mid-engagement) for a meaningful subset of users — long-term retainer
freelancers. Doesn't apply to gig/project-based users who renegotiate rates
per-project. Medium reflects the subset effect.

Today's workaround is to make a new project (loses history) or live with
mixed-rate weirdness.

**Implementation sketch:** new `RateChange` model with `effectiveDate: Date` +
`hourlyRate: Decimal`, owned by Project. TimeEntry.amount computation looks up
the project's rate as of `startedAt`. UI: a "Rate history" subsection on the
project editor with add/remove rows. Migration: existing project rate becomes
a single RateChange with `effectiveDate = .distantPast`.

### B3 — "What did I track today?" Siri intent — XS — **Low**

**Value rationale:** Voice surface for users who already invoke Siri daily.
The base of users who'd actually use this is small. Cheap to ship on top of
existing App Intent infrastructure — a delight feature rather than a value
driver. Tags as "nice for the App Store screenshot reel."

**Implementation sketch:** new `AppIntent` returning `IntentResult & ProvidesDialog`.
Aggregator queries today's `TimeEntry` rows from the shared App Group store,
formats hours + dollars + top client names, returns `.result(dialog: ...)`.
Donate on first invocation so Siri learns the phrasing.

### B4 — Stop button in Live Activity / Dynamic Island — XS — **High**

**Value rationale:** Removes friction from the single most-common timer action
on the most-common surface (lock screen). Currently every stop requires unlock +
app launch. Universal benefit, every day, every user with a running timer.
XS cost, High value — the best ratio in the backlog.

**Implementation sketch:** in the Live Activity widget (in `Widgets/`), wrap the
trailing area in a `Button(intent: StopTimerIntent())` with a stop icon.
Existing `StopTimerIntent` is reused as-is. Requires iOS 17.0 baseline (already
the project minimum).

### B5 — watchOS minimum-viable companion — M — **Medium**

**Value rationale:** Marketing-first feature. Real daily-driver value for a
small Apple Watch power-user segment, but the bigger payoff is App Store
listing differentiation — "Apple Watch app included" on the product page,
screenshots of the complication on a watch face. No competitor in this segment
has it. Medium because Cadence's daily-use loop on Watch is genuinely limited
(no Watch invoicing).

One screen: currently-running timer + Stop button + "Start last project"
quick action. Complication: small ring/text with elapsed seconds. No editing,
no timeline, no invoicing.

**Implementation sketch:** new watchOS target. Shares `BillableCore` package.
Reads/writes the same App Group + CloudKit-mirrored SwiftData store. Single
SwiftUI view + complication. WatchConnectivity not required because both
devices write to CloudKit and read on each side.

### B6 — Pinch-to-zoom on Timeline — S — **Low**

**Value rationale:** Polish on a feature only a subset of users heavily uses
(the timeline editor). Most users start/stop the timer and rarely visit the
timeline canvas. Real value for the heavy-edit segment, low impact on the
broader user base. Worth doing eventually because timeline is the wedge UX,
but not next.

**Implementation sketch:** `MagnificationGesture` on the timeline canvas mutates
a `@State` pt-per-hour value. Clamp to `[60, 120]`. Persist last-used value
to `@AppStorage`. All existing block layout math already scales off a single
`pointsPerHour` constant — refactor it to a variable.

---

## Next step

When ready to act on any of these, invoke the brainstorming skill against the
specific item to produce a proper design doc and (after approval) an
implementation plan. This backlog itself does not transition to an
implementation plan — it's the menu, not the dish.
