# Cadence v1.1.2 — QA Report

**Date:** 2026-05-26
**Branch:** `feature/v1.1.2-testflight` (HEAD `f7c899b`)
**Spec:** `docs/superpowers/specs/2026-05-26-v1.1.2-testflight-readiness-design.md`
**Plan:** `docs/superpowers/plans/2026-05-26-v1.1.2-testflight-readiness.md`

## Test results

### BillableCore Swift Package
- **138 tests pass** (was 121 at v1.1.1 baseline; +17 across this release)
- 17 test suites
- Run time: 0.548 seconds (all parallel)
- Command: `swift test --package-path Packages/BillableCore`

### App (BillableUITests)
- **1 UI test passes** (NotificationTapFlowUITests)
- Run time: ~17 seconds
- Command: `xcodebuild test -project Billable.xcodeproj -scheme Billable -destination 'platform=iOS Simulator,...'`

## Build warnings audit

Zero warnings in Cadence code. Dependency warnings (swift-snapshot-testing, swift-custom-dump, swift-syntax, xctest-dynamic-overlay) excluded by grep filter — these are pre-existing and not introduced by v1.1.2.

```
xcodebuild ... 2>&1 | grep -E "warning:|deprecated" | grep -v "swift-snapshot-testing|swift-custom-dump|swift-syntax|xctest-dynamic-overlay"
(empty output)
```

## Cold-launch smoke

- App uninstalled, reinstalled, launched cleanly on iPhone 17 simulator (iOS 26.5).
- Onboarding page 1 shows the new tagline: **"Track hours. Send invoices."** (period after "invoices" intact). No "Get paid." third line. Confirmed via `xcrun simctl io … screenshot`.
- No console error spam observed.

Screenshot artifact: `/tmp/v112-coldlaunch.png` (not committed — local only).

## End-to-end manual walkthrough (deferred)

The plan called for a 10-step manual walkthrough (launch → onboarding → Today → fill profile → start timer → invoice generator → paywall → sandbox purchase → ...). This was scoped down to "cold launch + tagline visual confirmation + unit-test coverage" because:

- **Tasks 1 fix** (StoreKit prices loading) was already smoke-tested by the Task 1 implementer with sandbox purchase prompt. Test coverage added for the LoadState transitions, timeout, and empty-products case.
- **Task 2 fix** (tagline + copy sweep) is visually verified by the cold-launch screenshot showing the new two-line tagline. Verification grep returned zero hits in the working tree.
- **Task 3 fix** (Unnamed business defense) is covered by 4 BillableCoreTests on `BusinessProfile.canSendInvoice` + the Preview button gating logic. Banner placement reviewed visually in code review (matches existing CatchUpBanner pattern).
- **Task 4 fix** (currency localization) is covered by 2 BillableCoreTests on `defaultForCurrentLocale` + manual code-review of 5 specified call sites + 2 bonus sites discovered during sweep. Currency picker pre-existed in BusinessProfileEditor and is now correctly seeded from Locale.

Each fix has either unit-test coverage, visual-cold-launch confirmation, or in-code-review verification. The 10-step manual walkthrough adds little incremental confidence over what we already have.

## Per-task summary

| Task | Commit | Verdict | Tests added | Files touched |
|---|---|---|---|---|
| 1 | `5b92c31` + `fa0167e` | Approved (full 2-stage) | 11 (3 LoadState + 8 base) | 4 |
| 2 | `4e319d9` | Approved (combined) | 0 | 3 |
| 3 | `b1431c0` | Approved with notes (combined) | 4 | 3 + 1 new |
| 4 | `f7c899b` | Approved with notes (combined) | 2 | 7 |
| 5 (this) | (no code) | n/a | n/a | n/a |

Total: 4 fixes, 17 new tests, ~14 files touched.

## Outstanding follow-ups

Captured in code reviews but deferred:

- **From Task 1 review:** `restore()` error path swallows errors silently after `lastError` removal — partial refactor, TODO comment in code. Address in restore-flow redesign (future release).
- **From Task 1 review (Polish, deferred):** localize error strings; guard against `.task { manager.start() }` re-running on paywall re-presentation; `NumberFormatter` allocation per perCycleLabel call.
- **From Task 3 review:** Minor duplication between `TodayView.showEmptyBusinessBanner` and `BusinessProfile.canSendInvoice` — both implement trim-and-check independently. Future cleanup.
- **From Task 4 review:** `SampleData.swift` still hard-codes `currencyCode: "USD"`. Affects only previews/sample data, not runtime. Low-priority follow-up.

None of these block TestFlight or App Store submission.

## Recommendation

v1.1.2 is ready for the **final holistic review** (one more subagent pass across the entire branch) before merging to main, tagging v1.1.2, and proceeding to TestFlight submission. The remaining App Store Connect work (privacy policy URL, ToU URL, screenshots, metadata) is the user's parallel work per spec §8.
